<div class="row">

	<div class="col-xs-3">
		<h2>User info</h2>
		<%= render 'users/profile', user: current_user %>
		<!--メモ　右側の指定に注意。そこを間違えるとエラーが発生して表示されない -->
		<%= render 'relationships/followbutton', user: current_user %>

		<h2>New book</h2>
		<%= render 'books/newform', book: @book %>
	</div>

	<!--users一覧 -->
	<div class="col-xs-9">
		<h2>Users</h2>
		
		<%= render 'users/index', users: @users %>
	</div>
<!--users一覧 -->

</div>